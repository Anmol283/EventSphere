<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(135deg, #f9f9f9 0%, #eaeaea 100%);
            color: #212529;
            font-family: 'Poppins', sans-serif;
            min-height: 100vh;
        }

        .glass {
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(0, 0, 0, 0.05);
            border-radius: 20px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
        }

        .event-hero {
            position: relative;
            height: 400px;
            border-radius: 20px;
            overflow: hidden;
            margin-bottom: 2rem;
        }

        .event-hero img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .event-hero-overlay {
            position: absolute;
            bottom: 0;
            left: 0;
            right: 0;
            background: linear-gradient(to top, rgba(0,0,0,0.8), transparent);
            padding: 2rem;
            color: white;
        }

        .event-date-badge {
            background: linear-gradient(135deg, #001F54, #003566);
            color: white;
            padding: 1rem 1.5rem;
            border-radius: 15px;
            display: inline-block;
            margin-bottom: 1rem;
        }

        .info-card {
            background: white;
            padding: 1.5rem;
            border-radius: 15px;
            border: 1px solid rgba(0, 0, 0, 0.05);
            margin-bottom: 1rem;
        }

        .info-card i {
            color: #001F54;
            font-size: 1.5rem;
            margin-right: 1rem;
        }

        .btn-primary {
            background: linear-gradient(135deg, #001F54, #003566);
            border: none;
            transition: all 0.3s ease;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, #003566, #001F54);
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.15);
        }

        .btn-outline-secondary {
            border-color: #001F54;
            color: #001F54;
        }

        .btn-outline-secondary:hover {
            background: #001F54;
            color: white;
        }

        .fade-in {
            animation: fadeIn 0.6s ease-in;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <main class="container py-5">
        <%- include('partials/flash-messages') %>

        <div class="fade-in">
            <!-- Back Button -->
            <a href="/events" class="btn btn-outline-secondary mb-4">
                <i class="fas fa-arrow-left me-2"></i>Back to Events
            </a>

            <!-- Event Hero Image -->
            <div class="event-hero glass">
                <img src="<%= event.image %>" alt="<%= event.title %>" onerror="this.src='https://images.unsplash.com/photo-1492684223066-81342ee5ff30?w=800&h=400&fit=crop'">
                <div class="event-hero-overlay">
                    <h1 class="display-4 fw-bold mb-0"><%= event.title %></h1>
                </div>
            </div>

            <div class="row g-4">
                <!-- Main Content -->
                <div class="col-lg-8">
                    <div class="glass p-4">
                        <h2 class="mb-4">
                            <i class="fas fa-info-circle text-primary me-2"></i>
                            Event Details
                        </h2>
                        <p class="lead"><%= event.description %></p>

                        <hr class="my-4">

                        <div class="row">
                            <div class="col-md-6">
                                <div class="info-card">
                                    <i class="fas fa-calendar-alt"></i>
                                    <div class="d-inline-block">
                                        <h6 class="mb-1">Date</h6>
                                        <p class="mb-0">
                                            <%= new Date(event.date).toLocaleDateString('en-US', { 
                                                weekday: 'long',
                                                year: 'numeric', 
                                                month: 'long', 
                                                day: 'numeric' 
                                            }) %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="info-card">
                                    <i class="fas fa-clock"></i>
                                    <div class="d-inline-block">
                                        <h6 class="mb-1">Status</h6>
                                        <p class="mb-0">
                                            <% if (new Date(event.date) > new Date()) { %>
                                                <span class="badge bg-success">Upcoming Event</span>
                                            <% } else { %>
                                                <span class="badge bg-secondary">Past Event</span>
                                            <% } %>
                                        </p>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Sidebar -->
                <div class="col-lg-4">
                    <div class="glass p-4">
                        <h4 class="mb-4">
                            <i class="fas fa-ticket-alt text-primary me-2"></i>
                            Event Info
                        </h4>

                        <div class="event-date-badge text-center w-100 mb-3">
                            <div class="fs-1 fw-bold">
                                <%= new Date(event.date).getDate() %>
                            </div>
                            <div>
                                <%= new Date(event.date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) %>
                            </div>
                        </div>

                        <% if (new Date(event.date) > new Date()) { %>
                            <a href="/contact" class="btn btn-primary w-100 mb-3">
                                <i class="fas fa-envelope me-2"></i>Contact for Registration
                            </a>
                        <% } %>

                        <div class="info-card">
                            <i class="fas fa-share-alt"></i>
                            <div class="d-inline-block">
                                <h6 class="mb-2">Share This Event</h6>
                                <div class="btn-group">
                                    <button class="btn btn-sm btn-outline-primary" title="Share on Facebook">
                                        <i class="fab fa-facebook-f"></i>
                                    </button>
                                    <button class="btn btn-sm btn-outline-primary" title="Share on Twitter">
                                        <i class="fab fa-twitter"></i>
                                    </button>
                                    <button class="btn btn-sm btn-outline-primary" title="Share on LinkedIn">
                                        <i class="fab fa-linkedin-in"></i>
                                    </button>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- Related Events -->
                    <div class="glass p-4 mt-4">
                        <h5 class="mb-3">
                            <i class="fas fa-list text-primary me-2"></i>
                            More Events
                        </h5>
                        <a href="/events" class="btn btn-outline-secondary w-100">
                            <i class="fas fa-calendar me-2"></i>View All Events
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>